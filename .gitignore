# env files — never commit secrets
.env
.env.local
.env.*.local
.env.production

# Next.js
.next/
out/

# dependencies
node_modules/

# build output
/build

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# persona-sprint: generated persona data stays local (generate_scaffold.py itself is still tracked)
persona-sprint/personas/personas.jsonl
persona-sprint/personas/scaffold.jsonl
persona-sprint/personas/batches/

# persona-sprint: generated feedback data stays local (themes_final.json itself is still tracked)
persona-sprint/feedback/feedback.jsonl
